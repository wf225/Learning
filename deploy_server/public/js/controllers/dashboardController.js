'use strict';

define([

], function () {

    function DashboardController($scope, $http, $timeout, $location ) {
        if (!$scope) {
            throw new Error('Argument $scope is invalid.');
        }
        
    }
    
    return DashboardController;
});